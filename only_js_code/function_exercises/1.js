// 1) Crie uma função que dado dois valores (passados como parâmetros) mostre no console a soma, subtração,
// multiplicação e divisão desses valores.

function all_in_one(a,b) {
    console.log(a+b,a-b,a*b,a/b)
}

all_in_one(8,3)